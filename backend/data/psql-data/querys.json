{
    "select": "SELECT * FROM persona",
    "where": "SELECT * FROM persona WHERE nombre_per = $1",
    "insert_username": "INSERT INTO persona (nombre_per, apellido_per, direccion_per) VALUES ($1, $2, $3)"
}